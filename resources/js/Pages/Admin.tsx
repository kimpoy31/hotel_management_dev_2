import Card from "@/components/Card";
import React from "react";

interface Props {}

const Admin = ({}: Props) => {
    return (
        <div>
            <Card></Card>
        </div>
    );
};

export default Admin;
